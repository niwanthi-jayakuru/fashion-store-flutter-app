# Fashion Store Assignment — TODO

## Phase 2 (Final Submission) — Must Deliver

- [ ] **Firebase setup (Android)**
  - [ ] Ensure `android/app/google-services.json` exists (generated by FlutterFire / Firebase Console).
  - [ ] Firebase Console → **Authentication** → enable **Email/Password**.
  - [ ] Firebase Console → **Firestore Database** → create database.
  - [ ] Confirm app runs on Android emulator/device with Firebase connected.

- [ ] **Pre-load products into Firestore (required)**
  - [ ] Use in-app seeder: **Profile → Seeder (Admin) → Seed Database**.
  - [ ] Verify `products` documents include: `name`, `price`, `category`, `image`, `description`.

- [ ] **End-to-end functional demo check**
  - [ ] Register a new user.
  - [ ] Login/logout works.
  - [ ] Browse products and filter by category.
  - [ ] Open product details and add to cart.
  - [ ] Cart: update quantity, remove item, total updates.
  - [ ] Checkout: enter phone + address → place order.
  - [ ] Order History: confirm new order appears for that user.
  - [ ] Profile: update display name/phone/address and confirm saved.

- [ ] **Build APK**
  - [ ] Run: `flutter build apk --release`
  - [ ] Confirm output exists: `build/app/outputs/flutter-apk/app-release.apk`

- [ ] **Technical Report (max 10 pages)**
  - [ ] Cover page + student details.
  - [ ] Introduction.
  - [ ] System Overview.
  - [ ] UI/UX Design (include key screens + user flow).
  - [ ] System Architecture (Flutter + Firebase components).
  - [ ] Database Design (collections: `products`, `orders`, `users`).
  - [ ] Implementation (key features and how they work).
  - [ ] Testing and Results (what you tested + screenshots if allowed).
  - [ ] Challenges and Solutions.
  - [ ] Conclusion and Future Improvements.
  - [ ] References.

- [ ] **Video Demonstration (3–5 minutes)**
  - [ ] Quick intro (name + module).
  - [ ] App overview.
  - [ ] Login/Register.
  - [ ] Product browsing + category filtering.
  - [ ] Cart → Checkout.
  - [ ] Order history.
  - [ ] Mention/Show Firebase integration (Firestore + Auth).
  - [ ] Show running on emulator/device.

## Repo Cleanup (important before submitting ZIP)

- [ ] **Ignore generated files**
  - [ ] Add/update `.gitignore` to exclude: `.dart_tool/`, `build/`, and other generated artifacts.
  - [ ] Delete any accidentally generated local browser/device profile files under `.dart_tool/` from the workspace (do not submit them).

## Optional Improvements (nice-to-have, only if time)

- [ ] Add loading/empty states everywhere consistently.
- [ ] Add better form validation messages (register/login/profile).
- [ ] Improve UI polish (consistent spacing, typography, colors).